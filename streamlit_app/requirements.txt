# Streamlit web layer for the NL Scenario Copilot.
# The core project (src/) already depends on anthropic, pandas, reportlab,
# python-dotenv; those are pinned in the project's own requirements. This file
# adds the web layer and re-declares the shared deps so the deployed app is
# self-contained.

streamlit>=1.40,<2.0
anthropic>=0.40
pandas>=2.0
reportlab>=4.0
python-dotenv>=1.0
